{
  "name": "mockoon-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm build && now ./build",
    "alias": "cd ./build && now alias"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mockoon/cli": "0.1.0-beta.2"
  }
}